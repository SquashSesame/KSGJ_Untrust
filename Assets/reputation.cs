using UnityEngine;

public class reputation : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public int reputationInt;
    void Start()
    {
       reputationInt = 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
